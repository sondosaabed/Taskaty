<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Taskaty"
        tools:targetApi="31">

        <!--    Informational package    -->
        <activity android:name="com.taskaty.informational.GettingStarted" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.taskaty.informational.WelcomeBack" android:exported="true"/>
        <activity android:name="com.taskaty.informational.StatusInform" android:exported="false"/>

        <!--    TaskMangamnet package    -->
        <activity android:name="com.taskaty.taskManagment.TasksList" android:exported="false"/>
        <activity android:name="com.taskaty.taskManagment.AddNewTask" android:exported="true"/>
        <activity android:name="com.taskaty.taskManagment.UpdateTask" android:exported="true"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
</manifest>